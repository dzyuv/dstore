spring:
  application:
    name: order-consumer8091
  cloud:
    openfeign:
      circuitbreaker:
        enabled: true
server:
  port: 8091
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true